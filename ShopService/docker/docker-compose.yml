version: '2'
services:
   shopserivce-1:
       container_name: shopserivce-1
       build:
          context: .
          dockerfile: Dockerfile
       restart: always
       volumes:
       - ../target/ShopService.jar:/root/target/ShopService.jar